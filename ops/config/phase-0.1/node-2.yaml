node:
  name: node-2
  listenHost: 0.0.0.0
  listenPort: 7000
  identityPath: /data/identity
peers:
  - host: pubsub-node-1
    port: 7000
  - host: pubsub-node-3
    port: 7000
transport:
  pingIntervalMs: 5000
  pingTimeoutMs: 3000
  reconnectInitialMs: 1000
  reconnectMaxMs: 10000
registry:
  enabled: true
  runtimeDir: /registry
  signer: node-2
  pollIntervalMs: 1000
control:
  host: 0.0.0.0
  port: 8000
